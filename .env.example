# Cookie/session secrets
SESSION_SECRET=change-me

# Optional Postgres (enables Prisma delegates)
# Example: postgresql://user:pass@localhost:5432/thefunny
DATABASE_URL=

# SMTP (optional; used for transactional emails if wired up)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="the-funny <no-reply@thefunny.local>"

# Open mic ingestion (optional)
GOOGLE_CSE_KEY=
GOOGLE_CSE_ID=
OPENMIC_ALLOWED_SITES=
DEFAULT_CITIES=Olympia,WA;Tacoma,WA;Seattle,WA
DEFAULT_RADIUS_MILES=50
INGEST_WINDOW_DAYS=60
ENABLE_REDDIT_OPENMICS=false

# Vercel host to allow Next.js server actions from (hostname only)
NEXT_PUBLIC_VERCEL_URL=
